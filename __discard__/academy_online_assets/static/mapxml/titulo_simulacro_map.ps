/watermarkText { (###) } def
/watermarkFont { /Arial_Bold 12 selectfont } def
/watermarkColor { .1 setgray } def
/watermarkAngle { 0 } def
/pageWidth { currentpagedevice /PageSize get 0 get } def
/pageHeight { currentpagedevice /PageSize get 1 get } def

<<
    /EndPage {
        2 eq { pop false } 
        {
            gsave
            watermarkFont
            watermarkColor
            221 653 moveto 0 rotate 
            watermarkText show
            grestore 
            true 
        } ifelse
    } bind
>> setpagedevice